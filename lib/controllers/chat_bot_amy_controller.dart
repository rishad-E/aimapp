// import 'package:get/get.dart';

// class ChatBotAmyController extends GetxController {
  // TextEditingController chatController = TextEditingController();
  // ScrollController scrollController = ScrollController();
  // List<ChatMessage> msgs = [];
  // bool isTyping = false;

  /* ------sending message----- */
  // Future<void> sendMsg(String text) async {
  //   String? res = await ChatBotService().sendMessageFunction(text);
  //   log(res.toString(), name: 'chat bot res c');
    // DateTime now = DateTime.now();
    // String currentTime = DateFormat('h:mm a').format(now);
    // log(currentTime);
  //   ChatMessage model = ChatMessage(true, text, currentTime);
  //   msgs.add(model);
  //   ChatMessage ai = ChatMessage(false, res.toString(), currentTime);
  //   msgs.add(ai);
  //   update(['chat-update']);
  // }
// }
